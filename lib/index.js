// 
const BlockChain = require('./models/BlockChain');

console.log('Creating new block chain');
const blockChain = new BlockChain();

console.log('Creating new block');
// const block = BlockChain.createGenesisBlock();

console.log('Adding block to chain, printing block and printing chain');
// blockChain.addBlock(blox)

console.log(blockChain);
